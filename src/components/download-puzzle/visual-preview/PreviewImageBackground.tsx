
import { ReactNode } from "react";

interface PreviewImageBackgroundProps {
  children: ReactNode;
}

export const PreviewImageBackground = ({ children }: PreviewImageBackgroundProps) => {
  return <>{children}</>;
};
